# Copy to .env and fill in values.
# Load with: export $(grep -v '^#' .env | xargs)

# ── SQLite (no config.yaml needed) ──────────────────────────────────────────
SQLITE_PATH=/var/lib/grafana/grafana.db
SQLITE_NAME=grafana-sqlite           # label shown in backup folder name

# ── MySQL (no config.yaml needed) ───────────────────────────────────────────
# MYSQL_HOST=127.0.0.1
# MYSQL_PORT=3306
# MYSQL_USER=grafana
# MYSQL_PASSWORD=changeme
# MYSQL_DATABASE=grafana
# MYSQL_NAME=grafana-mysql

# ── Per-database secrets (referenced in config.yaml via ${VAR}) ───────────
# GRAFANA_DB_PASS=changeme
# GRAFANA_PROD_HOST=prod-db.internal
# GRAFANA_PROD_USER=backup_user
# GRAFANA_PROD_PASS=changeme

# ── Output ───────────────────────────────────────────────────────────────────
BACKUP_OUTPUT_DIR=./backups
